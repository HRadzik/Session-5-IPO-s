#Hubert Radzik
#input phase
last_name = input("Enter employee last name: ")
salary = float(input("Enter salary: "))
level = int(input("Enter job level: "))
#processing
if level >= 10: bonus_rate = 0.25
else:
  if 5 <= level <= 9: bonus_rate = 0.20
  else:
      bonus_rate = 0.10
bonus = salary * bonus_rate
#output phase
print("Employee Last Name:", last_name)
print("Bonus: $", round(bonus, 2))
